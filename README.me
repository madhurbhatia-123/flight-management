# Flight Service Project

This project implements a simple Spring Boot application for managing airline flights.

## Prerequisites

- Java 17
- Maven

## Build and Run

```bash
# Clone the repository
git clone <repository-url>

# Navigate to the project directory
cd flight-service

# Build the project
mvn clean install

# Run the application
mvn spring-boot:run
